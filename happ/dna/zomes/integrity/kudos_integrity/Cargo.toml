[package]
name = "kudos_integrity"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
name = "kudos_integrity"

[dependencies]
hdi = { version = "0.5", features = ["mock"] }
serde = { version = "1", features = ["derive"] }
